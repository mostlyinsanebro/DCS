  // Partition the arry arr[] around the pivot such that elements smaller to it are on it's left and greater than it are on the right.
  Hint:- 0 -> j-1 (Smaller than pivot <=pivot)
         j -> i-1 (Greater than pivot  >pivot)
         i -> end (Unknowns)
         
  public static void partition(int[] arr, int pivot){
    //write your code here
    int i=0,j=0,temp=0;
    
    
    while(i!=arr.length)
    {
        if(arr[i]>pivot)
        {
            i++;
        }
        else if(arr[i]<=pivot)
        {
            System.out.println("Swapping "+arr[i]+" and "+arr[j]);
            temp=arr[i];
            arr[i]=arr[j];
            arr[j]=temp;
            
            i++;
            j++;
        }
    }
    
    
  }
